# send-benchmarking
The repo for a SEND benchmarking web-tool that uses publicly available data built using Django.

# Running locally
- pip install poetry
- pip install poetry-plugin-shell
- poetry install
- poetry shell
- python manage.py runserver
